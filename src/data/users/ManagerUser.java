package data.users;

public class ManagerUser extends User {
    public ManagerUser(String firstName, String secondName, String username, String password) {
        super(firstName, secondName, username, password);
    }
}
